# CLAUDE.md

本檔案為此專案的工作規則說明。

**專案**：TMSC 公司網站，GitHub Pages 靜態網站
**線上網址**：www.14064.com.tw
**Repo**：TMSC-Aaron/14064（分支 main）
**工作資料夾**：C:\Users\s0931\14064
**工作機**：Frieren 5070Ti 桌機（SSH 金鑰）／ASUS 筆電（HTTPS + Token）
**終端機**：Git Bash

---

## 一、Git 操作鐵則（違反將造成實質損害，最高優先）

### 1-1 禁止批次加入檔案

嚴禁使用 `git add .` 或 `git add -A`，一律指名檔案：

```bash
git add index.html
```

**理由**：本專案跨兩台 Windows 裝置作業，CRLF 換行差異會使未經修改之檔案被 git 誤判為 modified。115.08.08 曾發生 8 個檔案假性異動（新增／刪除行數完全相等 3188/3188），若逕行 `git add .` 將把三個加密目錄整份重寫。

**辨識方法**：`git diff --stat` 若新增與刪除行數相等，再以 `git diff --ignore-cr-at-eol` 複驗，即可分辨真假差異。

### 1-2 commit 前必須目視確認

執行 commit 前先跑 `git status`，確認暫存區僅含預期檔案，不得略過此步驟。

### 1-3 開工先 pull

每次對話開始處理任何修改前，先執行 `git pull` 確保是最新版本。

### 1-4 提交訊息使用繁體中文

每次修改完成、push 前，先用中文摘要說明改了什麼。commit message 一律以繁體中文撰寫，具體描述異動內容。

### 1-5 禁止危險操作

未經使用者明確指示，不得執行下列指令：

- `git reset --hard`
- `git push --force` / `git push -f`
- `git filter-branch`
- `git clean -fd`

---

## 二、受保護目錄

### 2-1 加密目錄（任何情形均不得納入提交）

| 目錄 | 說明 |
| --- | --- |
| `/yeezen/` | 怡仁綜合醫院版計算器（StatiCrypt 加密） |
| `/warroom/` | TMSC 內部戰情室（StatiCrypt 加密） |

上列目錄不得更動其加密狀態，不得重新產生，不得納入任何 commit。

### 2-2 內部展示目錄（可更新，須維持加密）

| 目錄 | 說明 |
| --- | --- |
| `/test/` | 無菌環境冷負荷與碳排計算器（最新版），供內部 demo 使用 |

**115.08.09 規則變更**：`/test/` 由「不得重新產生」改為「可更新」。使用者需要 demo 時會要求更新版本。更新時須維持下列條件，缺一不可：

1. StatiCrypt 加密，鹽值沿用 `.staticrypt.json`
2. `index.html` 與 `manual.html` **兩者都要加密**——說明書載有顯熱／潛熱分離之核心判斷，僅加密計算器等同未加密
3. 頁面須含 `noindex,nofollow,noarchive`
4. `robots.txt` 須保留 `Disallow: /test/`
5. 首頁及任何頁面**不得設置連結指向 `/test/`**，僅供知道網址者直接輸入

目錄結構：`index.html`（加密）、`manual.html`（加密）、`weather/*.csv`（五都全年 8760 小時，明文，公開氣象資料無保護必要）、`example-hourly.csv`（明文範例檔）。氣象 CSV 必須維持明文，加密後程式將無法讀取。

**StatiCrypt 注意事項**：Vite 建置之應用程式，其演算法邏輯位於 `assets/` 底下的獨立 JS 檔，僅加密 `index.html` 會使公式外洩。正確作法為先將 JS 內聯至 HTML，成為單一自足檔案後再加密。115.08.09 已依此法處理 `/test/`，主程式 230 KB 全數內聯，無 `assets/` 目錄。

### 2-3 限期開放目錄（修改前須確認）

| 目錄 | 說明 |
| --- | --- |
| `/kuangtien/` | 光田綜合醫院 ISO 50001 文件檢視專區 |

此目錄非加密頁面，內容仍會隨文件定稿而更新，但因涉及第三方驗證（115/8/25），任何修改前須先向使用者確認。有效期標註為 115/8/20－115/8/26，三處（頁首、頁尾、登入頁）須保持一致。

---

## 三、作業原則

1. **一次處理一項任務**，push 前先向使用者確認。
2. 使用者以**繁體中文**溝通，所有回應與 commit 訊息均使用繁體中文。
3. 網頁交付前以程式**逐項驗證**，不倚賴目視：
   - 檔案連結是否實際存在
   - 錨點是否有對應 id
   - HTML 標籤是否配對
4. 修改 `robots.txt` 或 `sitemap.xml` 時，須確認受保護目錄均已列入 Disallow。
5. **本機端檔案修改（非 git 指令）優先交由 Claude Code 執行**，藉此讓仁哥熟悉 Claude Code 操作流程。來源討論（雲端對話）提供修改內容與說明，Claude Code 負責實際寫入本機檔案。

---

## 四、視覺識別

| 項目 | 規範 |
| --- | --- |
| 配色 | 綠 70%／黑 20%／白 10% |
| Logo（導覽列、favicon） | `images/logo.png`（圓形地球） |
| Logo（Hero 背景、戰情室） | `images/logo_TMSC.png`（毛筆書法，已去背） |
| 法國三色 | 僅極少量使用；藍色限戰情室科技感，紅色限單一「熱烈招生中」標章 |

---

## 五、待決事項提醒

處理相關頁面時，請主動提醒使用者下列未結事項：

1. 14064.com.tw 網域移轉鎖（智邦）尚未啟用
2. **焓差冷負荷之專利與公開揭露時程衝突（最高優先）**
   - 原始工具曾於 `calculator.suhwanjan.com` 公開上線，頁尾載明「MIT License、計算所用公式為公開工程知識」
   - 該站未被搜尋引擎收錄，亦查無公開的程式碼倉庫，實際揭露程度低，但技術上任何人輸入網址即可取得
   - 待辦：請林醫師關站前刪除 MIT 字樣、記錄關站日期；並攜三個問題諮詢專利師——可申請之標的為何、四場研討會各自公開日、該站是否構成公開揭露
   - 在專利師給出答覆前，網站可展示介面與成果，**不得公開公式、參數表與推導**
3. 焓差冷負荷介紹頁（規劃中）：採 7 張圖敘事（痛點＋焓差圖、換氣規範、室內外溫度、氣象資料擷取、電費碳排、年度總計）。**冰水主機效率設定、降溫／除濕耗能比例兩張圖暫緩**，後者揭露顯熱潛熱分離之核心判斷。文案原則：只給「考慮了什麼」，不給「怎麼算」，且不將換氣規範與焓差計算寫在同一句因果裡
4. 首頁 Hero 改版待議：設計師提供之線框稿（藍色為佔位色，非改配色）、`images/view.jpg` 底圖解析度僅 1043 px 需重出 2400 px 版、導覽列維持現狀不動
5. 英文版：導覽列不放假的語言切換鈕（宣稱與實際不符）。改做單頁 `en.html`，內容涵蓋公司簡介、四家醫院實績、四篇論文、IPC／IAS 證照班、聯絡方式
6. 刻印對聯「繼往聖之絕學／開永續之未來」規劃置於「關於我們」區塊開頭，非 Hero；Hero 文字量已足

### 已結案

- ~~`/warroom/` 加入 `robots.txt` Disallow~~ → 115.08.09 確認四個受保護目錄均已列入
- ~~`sitemap.xml` 重建~~ → 115.08.09 確認僅存首頁與 `privacy.html`

---

*最後更新：民國 115 年 8 月 11 日*
