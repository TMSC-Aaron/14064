# CLAUDE.md

本檔案為此專案的工作規則說明。

## 工作規則

1. 每次對話開始處理任何修改前,先執行 `git pull` 確保是最新版本。
2. 每次修改完成、push 前,先用中文摘要說明改了什麼。
3. 這是 GitHub Pages 靜態網站,線上網址是 www.14064.com.tw。
4. `yeezen/` 和 `test/` 是 StatiCrypt 加密頁面,不可以動它們的加密狀態。
